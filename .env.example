# Firebase Configuration
# Get these from Firebase Console: https://console.firebase.google.com

# Client-side Firebase config (safe to expose)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Server-side Firebase Admin config (KEEP SECRET!)
# Get from: Project Settings → Service Accounts → Generate New Private Key
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# UIA Security Secrets
# NEXT_PUBLIC_SECRET: Used for fingerprint encoding (client-side obfuscation)
NEXT_PUBLIC_SECRET=change-me-to-something-random

# SERVER_SECRET: Used for token generation (server-side, KEEP SECRET!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SERVER_SECRET=change-me-to-a-random-64-char-hex-string